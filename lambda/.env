# Local development overrides.
# In AWS Lambda set these as environment variables in the console / IaC — do NOT deploy this file.

AWS_REGION=eu-west-1

# DynamoDB
DYNAMODB_TABLE=auditoria-generacion-dev

# S3
S3_BUCKET=auditoria-context-dev
TEMPLATE_S3_KEY=templates/plantilla.docx
